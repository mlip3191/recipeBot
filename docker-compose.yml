services:
  recipes:
    build: .
    environment:
      - PORT=${PORT:-8000}
      - DATABASE_URL=${DATABASE_URL:-}
    ports:
      - "${PORT:-8000}:${PORT:-8000}"
    volumes:
      - recipe_data:/data
      - ./backups:/backups
    restart: unless-stopped

volumes:
  recipe_data:
